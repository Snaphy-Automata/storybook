import {
    ON_ADD_MORE_SECTIONS,
    ON_SECTION_EXPANDED
} from './TaskSectionsAction';


const initialState = {
   
};


const TaskSectionsReducer = (state = initialState, action) => {
    switch(action.type){
        
    }

    return state;

}

export default TaskSectionsReducer;